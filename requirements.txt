# ROKER NEXUS — Requirements con versiones compatibles verificadas
# Orden importa: resolver conflictos httpx primero

streamlit==1.32.2
pandas==2.1.4
openpyxl==3.1.2
xlrd==2.0.1
plotly==5.18.0
numpy==1.26.4

# HTTP — versión única compatible con supabase + telegram
httpx==0.27.0
requests==2.31.0
aiohttp==3.9.3

# AI providers — opcionales, no crashean si no están
anthropic==0.25.0

# Telegram
python-telegram-bot==21.1.1

# Supabase — versión que funciona con httpx 0.27
supabase==2.4.2

# Matching difuso
rapidfuzz==3.6.1

# Fechas y zonas horarias
python-dateutil==2.9.0
pytz==2024.1

# Utils
python-dotenv==1.0.1

# Google APIs (opcional — solo si se usan)
# google-generativeai==0.5.0
# google-auth==2.28.0
# google-auth-oauthlib==1.2.0
# google-api-python-client==2.118.0
